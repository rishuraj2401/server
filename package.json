{
  "name": "cls",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js",
    "dev": "node ./app.js",
    "start": "node ./app.js",
    "heroku-postbuild": "CI=false && npm install && cd my-project && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "bcryptjs": "^2.4.3",
    "body": "^5.1.0",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.10",
    "nodemon": "^2.0.15",
    "parser": "^0.1.4",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}